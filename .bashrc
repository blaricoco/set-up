# CONFIGURATION: COLOR
alias ls='ls --color=auto'
alias ll='ls -la --color=auto'

# CONFIGURATION: BASH
alias src='source ~/.bashrc'
alias notes="cd $NOTES_DIR/notes ; vim README.md"
alias setup="cd $HOME_DIR/set-up; ll"
alias scripts="cd $SCRIPTS_DIR ; ll"

# CONFIGURATION: FILTERS

# CONFIGURATION: DIARY
 
# CONFIGURATION: TASKS

